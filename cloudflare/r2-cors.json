{
  "rules": [
    {
      "allowed": {
        "origins": [
          "http://localhost:3000",
          "https://syneautonomous.cloud",
          "https://*.syneautonomous.cloud",
          "https://*.vercel.app"
        ],
        "methods": ["GET", "PUT", "HEAD"],
        "headers": ["*"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
